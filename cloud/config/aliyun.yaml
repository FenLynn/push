# Aliyun 环境配置 - 覆盖 default.yaml 中的值
# 主机名: iZ*, aliyun, 或 IP 47.*, 8.*

network:
  # 阿里云使用远程 TTR RSS
  ttrss_url: "http://47.109.56.199:12002"
  
  # 如果需要代理（可选）
  # http_proxy: "http://proxy.example.com:8080"

# 其他配置继承 default.yaml
